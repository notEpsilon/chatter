{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "rm -rf dist && npx tsc && node dist/index.js",
    "build1": "cd client/chatter && npm install",
    "build2": "cd client/chatter && npm run build",
    "heroku-postbuild": "npm run build1 && npm run build2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/socket.io": "^3.0.2",
    "express": "^4.17.1",
    "socket.io": "^4.1.2"
  },
  "devDependencies": {
    "typescript": "^4.3.5"
  }
}
